version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install pipenv
      - pipenv install 
      - pip --version
      - pip install -r requirements.txt
run:
  command: python app.py
  network:
    port: 8000


